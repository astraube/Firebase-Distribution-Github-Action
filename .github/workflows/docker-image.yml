name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-docker-image:
    runs-on: ubuntu-latest
    steps:
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: "straucorp"
        password: "dckr_pat_DfI2OokaoWFlCP21bWbskV4t9HU"
        
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
            docker login --username straucorp --password dckr_pat_DfI2OokaoWFlCP21bWbskV4t9HU
            docker build . --file Dockerfile --tag firebase-distribution-github-action:$(date +%s)
            docker push straucorp/firebase-distribution-github-action:$(date +%s)
